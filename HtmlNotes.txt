Html(Hyper Text Markup Language) - structure/layout
Css(Cascading Style Sheets) - Style
Js - Logic
-----------------------------------------------------------------
Html is the code that is used to structure a web page and its content.
The component used to design the structure of websites are called HTML tags.

Html Tag - A container for some content or other HTML tags
-----------------------------------------------------------------
<!DOCTYPE html> tells browser you are using HTML 5
<html> root of an html document
<head> container for metadata
<title> page title
<body> contains all data rendered by the browser
<p> paragraph tag
-----------------------------------------------------------------
“Metadata in the <head> gives information about the webpage to browsers and search engines. It helps with SEO, proper display (like charset), and responsiveness (viewport).”
-----------------------------------------------------------------
HTML is not case sensitive

<html>=<HTML>
<p>=</p>
<body>=</body>
----------------------------------------------------------------
HTML Attributes
Attributes are used to add more information to the tag
<html lang="en">
----------------------------------------------------------------
Heading Tag- Used to display headings in HTML
h1 (most important), h2, h3, h4, h5, h6 (least important)

**Don't use bigger tags h1 for font size, give bigger tags like h1 for importance. Because it can worse SEO
---------------------------------------------------------------
Paragraph Tag- used to add paragraphs in HTML
---------------------------------------------------------------
Anchor tag - used to add links to your page

<a href=" abcdef "> Something </a> here href is an attribute -absolute link means going to another page

<a href="/ abcdef"> Something </a> -relative link cause they are in same folder
---------------------------------------------------------------
Image Tag- Used to add images to your page
<img src=" abcd " alt= "something"> -> You can use relative link also
alt - if some error come render the name in the browser
--------------------------------------------------------------
<br> - Used to add next line(line breaks)to your page
-------------------------------------------------------------
Bold, italic & underline Tags - Used to highlight text in your page
-------------------------------------------------------------
Big & Small Tags - Used to display big & small text on your page
--------------------------------------------------------------
Hr Tag - Used to display a horizontal ruler, used to separate content
-------------------------------------------------------------
Subscript & Superscript Tag - Used to display a horizontal ruler, used to separate content
-------------------------------------------------------------
Pre tag- Used to display text as it is (without ignoring spaces & next line)
--------------------------------------------------------------
Layouts - Tags wont affect on website, but helpful work internally and seo rankings heavy impact.

Semantic Tags are HTML elements that clearly describe their meaning to both the browser and the developer.

<header>
<main> 
<footer>
-------------------------------------------------------------------
Div Tag = Div is a container used for other HTML elements
BLOCK Element(takes full width).
DIV is an non-semantic tag

It will take full row
and if you want to change color for sull section of code then use div tags

------------------------------------------------------------------
Span Tag = Span is also a container used for other HTML elements
Inline Element (takes width as per size)


